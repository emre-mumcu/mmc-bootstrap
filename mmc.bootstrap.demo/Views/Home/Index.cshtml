@model string

<partial name="AppNavbar" />

@if (string.IsNullOrEmpty(Model))
{
    <div class="row my-4">

        <div class="col">
            <strong>Bootstrap Components</strong>

            <ol class="mt-2">
                <li>
                    <strong><a asp-route-id="Accordion">Accordion</a></strong>
                    <p>Build vertically collapsing accordions</p>
                </li>
                <li>
                    <strong><a asp-route-id="Alert">Alert</a></strong>
                    <p>Provide contextual feedback messages</p>
                </li>
                <li>
                    <strong><a asp-route-id="Badge">Badge</a></strong>
                    <p>Provide contextual feedback messages</p>
                </li>
                <li>
                    <strong><a asp-route-id="BreadCrumb">BreadCrumb</a></strong>
                    <p>Indicate the current page’s location within a navigational hierarchy that automatically adds
                        separators via CSS.</p>
                </li>
            </ol>
        </div>
        
        <div class="col">
            <strong>Bootstrap Components</strong>

            <ol class="mt-2">
                <li>
                    <strong><a asp-route-id="Accordion">Accordion</a></strong>
                    <p>Build vertically collapsing accordions</p>
                </li>
                <li>
                    <strong><a asp-route-id="Alert">Alert</a></strong>
                    <p>Provide contextual feedback messages</p>
                </li>
                <li>
                    <strong><a asp-route-id="Badge">Badge</a></strong>
                    <p>Provide contextual feedback messages</p>
                </li>
                <li>
                    <strong><a asp-route-id="BreadCrumb">BreadCrumb</a></strong>
                    <p>Indicate the current page’s location within a navigational hierarchy that automatically adds
                        separators via CSS.</p>
                </li>
            </ol>
        </div>


    </div>
}
else
{
    <partial name="Container" model="@Model" />
}

<assembly-version />

@section body {
    <script src="https://cdn.jsdelivr.net/gh/highlightjs/cdn-release@11.9.0/build/highlight.min.js"></script>
    <script>hljs.highlightAll();</script>
    <script src="@Url.Content("~/js/app.js")"></script>
}